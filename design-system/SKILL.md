---
name: optihealth-design
description: Use this skill to generate well-branded interfaces and assets for optiHealth — an N-of-1 biohacking platform — either for production or throwaway prototypes/mocks/etc. Contains essential design guidelines, colors, type, fonts, assets, and UI kit components for prototyping. Covers BOTH the liquid-glass Next.js web.
user-invocable: true
---

Read the `README.md` file within this skill, and explore the other available files. Start with `README.md` for product context and the VISUAL FOUNDATIONS / CONTENT FUNDAMENTALS sections; then `colors_and_type.css` for tokens; then `ICONOGRAPHY.md` for icon strategy per product.

**Two products, two visual languages — pick one before you start designing:**
- **Web** (liquid glass, indigo gradient, Geist + Lucide) → use `ui_kits/web/` components.

If creating visual artifacts (slides, mocks, throwaway prototypes, etc), copy assets out of `assets/` and `ui_kits/` and create static HTML files for the user to view. Reference `colors_and_type.css` directly. If working on production code, read the tokens and component patterns to match the real app.

Then act as an expert optiHealth designer and output HTML artifacts or production code as appropriate. Keep voice empowering, second-person, outcome-focused; keep `optiHealth` cased correctly (lower-o, capital-H).
